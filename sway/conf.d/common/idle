### Idle configuration
# exec swayidle -w  timeout 900 'swaylock -f -i $HOME/pictures/wallpapers/solar_system_uw.png -s fill' \
#                   timeout 999 'swaymsg "output * dpms off"' \
#                       resume  'swaymsg "output * dpms on"' \
#                 before-sleep  'swaylock -f -i $HOME/pictures/wallpapers/solar_system_uw.png -s fill'
#
# This will lock your screen after 180 seconds of inactivity, then turn off
# your displays after another 60 seconds, and turn your screens back on when
# resumed. It will also lock your screen before your computer goes to sleep.
